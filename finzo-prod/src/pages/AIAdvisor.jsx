export default function AIAdvisor() {
  return (
    <div className="page" style={{textAlign:'center', paddingTop:48}}>
      <div style={{fontSize:48, marginBottom:16}}>🤖</div>
      <div style={{fontSize:18, fontWeight:800, color:'var(--text)', marginBottom:8}}>{'AI Advisor'}</div>
      <div style={{fontSize:13, color:'var(--sub)', maxWidth:360, margin:'0 auto', lineHeight:1.7}}>{'Full AI financial analysis, tax tips, and monthly report card — available after your first import.'}</div>
    </div>
  )
}